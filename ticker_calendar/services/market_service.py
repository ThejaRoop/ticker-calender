from __future__ import annotations

# Market service - yfinance API calls removed.
# This module is kept for compatibility but no longer makes external API calls.
# All alert logic now uses database and time-based checks only.