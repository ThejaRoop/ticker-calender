services:
  ticker-calendar:
    build: .
    container_name: ticker-calendar
    command: ["python", "run_server.py", "serve"]
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
      TICKER_CALENDAR_LOG_DIR: /app/logs
      TICKER_CALENDAR_DB: /app/data/ticker_calendar.db
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
